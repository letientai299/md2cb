# Inline Code Edge Cases

Text with `code` in middle.

`code at start` of line.

End of line with `code`

Multiple `code` blocks `in one` line `here`.

Backticks in code: `` `backtick` ``

Empty code: `` `` (should be empty)

Code with special chars: `<div class="test">&amp;</div>`
